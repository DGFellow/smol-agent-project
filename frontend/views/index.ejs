<%- include('header') %>

<main class="container">
    <div class="chat-interface">
        <div class="agent-indicator">
            <span class="agent-badge" id="current-agent">Ready</span>
            <span class="model-name" id="current-model"></span>
        </div>

        <div id="chat-messages" class="messages"></div>

        <div class="input-area">
            <textarea 
                id="message-input" 
                placeholder="Ask me anything or request code generation..."
                rows="3"
            ></textarea>
            <div class="button-group">
                <button id="send-btn" class="btn btn-primary">
                    <span class="btn-icon">📤</span> Send
                </button>
                <button id="clear-btn" class="btn btn-secondary">
                    <span class="btn-icon">🗑️</span> Clear
                </button>
            </div>
        </div>

        <div class="status">
            <span id="status-indicator" class="status-dot"></span>
            <span id="status-text">Ready</span>
        </div>

        <div class="help-text">
            <p><strong>💡 Tip:</strong> Just type naturally! I'll automatically detect if you need code or conversation.</p>
            <p><strong>Examples:</strong></p>
            <ul>
                <li>"Write a Python function to calculate fibonacci numbers"</li>
                <li>"Explain how neural networks work"</li>
                <li>"Create a REST API endpoint in Express"</li>
            </ul>
        </div>
    </div>
</main>

<%- include('footer') %>